'use client';

import PageHeader from "@/shared/components/page-header";
import { FileText } from 'phosphor-react';

export default function RulesPage() {
  return (
    <div>
      <PageHeader icon={FileText} title="Rules" />
    </div>
  );
}

